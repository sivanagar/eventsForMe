import React from "react";
import Button from "@mui/material/Button"

const Home = () => {
  return (
    <div>
        <a href="/sign-up">
        <Button variant="outlined">SIGN UP</Button>
          </a> 
    </div>
  );
};

export default Home;
